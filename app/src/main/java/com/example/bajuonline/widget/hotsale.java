package com.example.bajuonline.widget;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.LinearLayout;

import androidx.annotation.Nullable;

public class hotsale extends LinearLayout {
    public hotsale(Context context) {
        super(context);
    }

    public hotsale(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }

    public hotsale(Context context, @Nullable AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }

    public hotsale(Context context, AttributeSet attrs, int defStyleAttr, int defStyleRes) {
        super(context, attrs, defStyleAttr, defStyleRes);
    }
}
