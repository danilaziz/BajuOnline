package com.example.bajuonline.widget;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.LinearLayout;

import androidx.annotation.Nullable;

public class kategori_elemen extends LinearLayout {
    public kategori_elemen(Context context) {
        super(context);
    }

    public kategori_elemen(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }

    public kategori_elemen(Context context, @Nullable AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }

    public kategori_elemen(Context context, AttributeSet attrs, int defStyleAttr, int defStyleRes) {
        super(context, attrs, defStyleAttr, defStyleRes);
    }
}
